package withoutJersey;

import java.io.FileNotFoundException;

public class Main {

    public static void main(String[] args) throws FileNotFoundException {
    /*
        Config config = Config.getInstance();
        config.clear();
        config.getHashtags().put("gameofthrones", new ArrayList<String>());
        config.getHashtags().put("got", new ArrayList<String>());

        StreamManager streamManager = StreamManager.getInstance();


        streamManager.startStream(config);

        do {
            try {
                Thread.sleep(60000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        } while (true);
    */
        //streamManager.stopStream();

//
//		streamManager.startStream(config);
//		
//		streamManager.stopStream();
//		
//
//		streamManager.startStream(config);

//		streamManager.stopStream();
        //System.out.println(config);

    }

}
